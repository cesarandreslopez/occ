name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Build & Verify
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18, 20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Generate test fixtures
        run: node test/create-fixtures.js

      - name: Verify document scanning
        run: node dist/bin/occ.js test/fixtures/

      - name: Verify JSON output
        run: node dist/bin/occ.js --format json test/fixtures/

      - name: Verify CI mode
        run: node dist/bin/occ.js --ci test/fixtures/

      - name: Verify by-file mode
        run: node dist/bin/occ.js --by-file test/fixtures/

      - name: Verify no-code mode
        run: node dist/bin/occ.js --no-code test/fixtures/

      - name: Verify structure extraction
        run: node dist/bin/occ.js --structure test/fixtures/

      - name: Verify structure JSON output
        run: node dist/bin/occ.js --structure --format json test/fixtures/
